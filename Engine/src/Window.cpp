#include "Window.h"

namespace SoftEngine
{
	void Window::Init(uint32_t width, uint32_t height, uint32_t bufferWidth, uint32_t bufferHeight)
	{
		//glGenTexture(1, m_Buffer);
		//glBindTexture(GL_TEXTURE_2D, texture);
	}

	void Window::SwapBuffer(uint32_t width, uint32_t height, std::vector<uint32_t> buffer)
	{

	}
};
